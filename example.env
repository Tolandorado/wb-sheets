# PostgreSQL Configuration
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Application Configuration
APP_PORT=5000

# Wildberries API Configuration
WB_API_URL=https://common-api.wildberries.ru
WB_API_KEY=your_wb_api_token_here

# Google Service Account Configuration
# ВАЖНО: JSON должен быть закодирован в base64 или экранирован!
# 
# Вариант 1 (рекомендуется): Закодируйте JSON в base64
# В PowerShell: [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((Get-Content service-account.json -Raw)))
# В Linux/Mac: cat service-account.json | base64 -w 0
# 
# Вариант 2: Экранируйте кавычки и переносы строк
# Замените все " на \" и уберите все переносы строк
# 
# Пример base64 (замените на свой):
GOOGLE_SERVICE_ACCOUNT=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50In0=

# Google Spreadsheet IDs
# Укажите ID таблиц через запятую без пробелов
# ID можно найти в URL таблицы: https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
GOOGLE_SPREADSHEET_IDS=spreadsheet_id_1,spreadsheet_id_2

# Logging (опционально)
# LOG_LEVEL=info
